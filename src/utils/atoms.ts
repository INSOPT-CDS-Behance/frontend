import { atom } from 'recoil';

export const projectClicked = atom({
  key: 'projectClicked',
  default: false,
});

export const detailhover1Clicked = atom({
  key: 'detailhover1Clicked',
  default: false,
});

export const detailhover2Clicked = atom({
  key: 'detailhover2Clicked',
  default: false,
});
